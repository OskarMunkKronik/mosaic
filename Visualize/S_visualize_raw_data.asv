
%%
cd D:\people\mht541\rawData\nadine\SparseTensor\
f = dir("*200ppb*");
Z = cell(length(f),1);
for n = 1:length(f)
Z{n} = load(f(n).name);
end 
% cd D:\people\mht541\rawData\nadine\SparseTensor\
% f_blank = dir("*Blank*");
% % Z = cell(length(f_blank),1);
% for n = 1:length(f_blank)
% Z_blank{n} = load(f_blank(n).name);
% % Z{2} = load("D:\people\mht541\rawData\nadine\SparseTensor\~20250923-006-nPFAS+OCBs_200ppb_splitless.D_Img01.cdf.mat");
% end
%%
mzTarget =289.9188
 

;
clf
[~,a] = min(abs(Z{1}.mzroi_aug-mzTarget));
col = lines(length(f));
for k =1:length(f)
Zplot = double(Z{k}.Z(:,:,a))';
Zplot(Zplot == 0) = 0;
plot3(Options.coords.X,Options.coords.Y, Zplot)%,'Color',col(1,:))%, ...
        hold on
end 
% % clf
% [~,a] = min(abs(Z_blank{1}.mzroi_aug-mzTarget));
% % col = lines(length(f));
% for k =1:length(f_blank)
% Zplot = double(Z_blank{k}.Z(:,:,a))';
% Zplot(Zplot == 0) = nan;
% mesh(Options.coords.X, Options.coords.Y, Zplot,'Color',col(2,:))%, ...
%         hold on
% end 


%%
mzTarget = 218.9092

mz = mz_in_mass_spectra((cellfun(@isempty,mz_in_mass_spectra(:))));
ind_2222 = find(cell2mat(cellfun(@(mz_spec) ...
    any(abs(mzroi_aug(mz_spec) - mzTarget) ./ mzTarget * 1e6 < ppm_dev), ...
    mz_range(:), 'UniformOutput', false)));
mzroi_aug(mz_range{ind_2222(end)})
peak_borders{ind_2222}
% % ind =any(ind_2222' == feature_table(:,2),2);
% ind = abs((feature_table(:,1)) - mzTarget) ./ mzTarget * 1e6 < ppm_dev;
% [feature_groups_all(ind).Rt]
% feature_table(ind,2)
% sum(428870 ==  feature_table(:,2))
